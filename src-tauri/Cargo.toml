[package]
name = "ainode-desktop"
version = "0.2.0"
description = "AINode on your desktop: point it at your master node and go."
authors = ["AINode contributors"]
license = "Apache-2.0"
repository = "https://github.com/getainode/ainode-desktop"
edition = "2021"
rust-version = "1.85"

[lib]
name = "ainode_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "=2.6.3", features = [] }

[dependencies]
tauri = { version = "=2.11.5", features = ["tray-icon", "image-png", "image-ico"] }
tauri-plugin-store = "=2.4.5"
tauri-plugin-notification = "=2.4.0"
tauri-plugin-opener = "=2.5.5"
# native-tls rather than reqwest's default rustls: on macOS it is
# Security.framework and on Windows schannel, so "add the certificate to your
# trust store" is advice that actually works, and neither platform needs a build
# dependency for it (no cmake, no nasm, no vendored openssl in either release
# workflow). The app only ever verifies; nothing here can be told to skip it.
reqwest = { version = "=0.13.5", default-features = false, features = ["json", "native-tls"] }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
tokio = { version = "=1.53.1", features = ["time", "sync", "macros"] }
url = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
